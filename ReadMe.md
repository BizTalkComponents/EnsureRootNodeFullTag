The component ensures that the root node is not self-closing tag.
<br>
BizTalk pipeline component 'Xml disassembler' may fail in case it receives an empty envelope with self-closing root node tag.
<br>
